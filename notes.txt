public function crawl($url, $counter)
    {
        //$counter = $counter + 1;
        // Das hier soll umgebaut werden, in was anderes und vor allem es rekursiv zu bauen
        // anchor href: "//a/@href"
        // anchor src:  "//img/@src"
        $crawledData = array();
        $xpathQuery = new CurlQuery($url);
        $urlQuery = $xpathQuery->query("//a/@href"); // evtl. filern?
        //error_log(print_r($url, TRUE));
        //error_log(print_r($counter, TRUE));
//        if($counter == -1)
//        {
//            return;
//        } else {
            for($i = 0; $i < $urlQuery->length; $i++)
            {
                $nodeValue = $urlQuery->item($i)->nodeValue;
                $parsedNodeValue = parse_url($nodeValue);

                //TODO URL-Parser einbauen -> new urlparser($v)?
                if(!empty($parsedNodeValue['scheme']))
                {
                    // Absolute URL is not for interest
                } else {
                    // TODO relative URL $nodeValue ergänzen, um den Anfang von $url
                    // TODO herausfinden wo die eindeutige URL liegt und diese rekursiv neu aufrufen
                    //echo $nodeValue . "<br />";
                    $crawledData[]['url'] = $url . $nodeValue;
                    // für jeden Durchlauf in neues Array, nach jedem Durchlauf auf ein anderes Array mergen
                    $this->session->set('url', $crawledData);
                    //echo count($urlQuery) . "&nbsp;";
                    //echo count($crawledData) . "<br/>";
                    //$this->crawl($url . $nodeValue, $counter);
                    // Zum Tracen können Sie auch mal versuchen, jedesmal, wenn er eine Url gefunden hat, diese mit echo ausgeben
                    // Wenn url schon besucht push back wenn ja und return
                    // Dafür eine Liste anlegen und prüfen, ob diese vorhanden ist
                }
            }
       // }


    }

    <urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.sitemaps.org/schemas/sitemap/0.9 http://www.sitemaps.org/schemas/sitemap/0.9/sitemap.xsd">
    <!--  created with Free Online Sitemap Generator www.xml-sitemaps.com  -->
    <url>
    <loc>https://duckduckgo.com/</loc>
    <lastmod>2021-10-07T06:27:07+00:00</lastmod>
    <priority>1.00</priority>
    </url>


     // if(str_starts_with($link,'//')) {
                    //     echo $link;
                    //     echo '  Relativ ';
                    //     echo $solvedLink;
                    //     exit;
                    // }